name: image_picker_saver
description:
  Image Saver plugin for Flutter
  Android supported
  IOS supported 8.0+
  fork form package image_picker_saver
  and removed image_picker functionality

authors:
  - Aaron Ho <cnhefang@gmail.com>
  - Carl Gödecken

homepage: https://github.com/cnhefang/image_picker_saver
version: 0.1.0

flutter:
  plugin:
    androidPackage: com.mastercarl.imagesaver
    iosPrefix: FLT
    pluginClass: ImageSaverPlugin

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.0.0-dev.28.0 <3.0.0"
  flutter: ">=0.1.4 <2.0.0"
